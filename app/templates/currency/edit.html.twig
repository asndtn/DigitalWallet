{% include 'default/_form.html.twig' with {
    page_title: 'title_currency_edit',
    page_title_param: {'%id%': currency.id},
    attr: {
        method: 'PUT',
        action: url('currency_edit', {'id': currency.id}),
    },
    form: form,
    back_to_list_path: 'currency_index',
} %}