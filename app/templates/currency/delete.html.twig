{% include 'default/_form.html.twig' with {
    page_title: 'title_currency_delete',
    page_title_param: {'%id%': currency.id},
    attr: {
        method: 'DELETE',
        action: url('currency_delete', {'id': currency.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'currency_index',
} %}