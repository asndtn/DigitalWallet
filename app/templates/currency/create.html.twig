{% include 'default/_form.html.twig' with {
    page_title: 'title_currency_create',
    attr: {
        method: 'POST',
        action: url('currency_create')
    },
    form: form,
    submit_label: 'action_create'|trans,
    back_to_list_path: 'currency_index',
} %}