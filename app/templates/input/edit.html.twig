{% include 'default/_form.html.twig' with {
    page_title: 'title_input_edit',
    page_title_param: {'%id%': input.id},
    attr: {
        method: 'PUT',
        action: url('input_edit', {'id': input.id}),
    },
    form: form,
    back_to_list_path: 'input_index',
} %}