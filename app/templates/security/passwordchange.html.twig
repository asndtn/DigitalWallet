{% include 'default/_form.html.twig' with {
    page_title: 'title_change_password',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('app_changePassword', {'id': user.id}),
    },
    back_to_list_path: 'wallet_index',
} %}